# Do NOT commit a real .env file or real production credentials.
# Configure these values in the VPS/server environment.

DB_HOST=your-vps-host
DB_PORT=3306
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
